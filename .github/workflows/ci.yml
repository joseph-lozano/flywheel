name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-24.04
    outputs:
      code: ${{ github.event_name == 'push' || steps.diff.outputs.code == 'true' }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Detect changed files
        if: github.event_name == 'pull_request'
        id: diff
        env:
          BASE_REF: ${{ github.base_ref }}
        run: |
          CHANGED=$(git diff --name-only "origin/${BASE_REF}...HEAD")
          # Skip CI only when ALL changed files are known non-code files.
          # Unknown file types default to triggering CI (safer).
          if echo "$CHANGED" | grep -qvE '\.(md|png|svg|icns|gif|jpg|jpeg)$|^docs/|^LICENSE'; then
            echo "code=true" >> "$GITHUB_OUTPUT"
          else
            echo "code=false" >> "$GITHUB_OUTPUT"
          fi

  lint-and-test:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-24.04
    steps:
      - name: Check for expiring secrets
        run: |
          TODAY=$(date -u +%Y-%m-%d)
          if [[ "$TODAY" > "2026-05-31" ]]; then
            echo "::error::GH_TOKEN expires 2026-06-24. Rotate it now and update the secret."
            exit 1
          fi
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
          run_install: false
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm lint
      - run: pnpm test

  build:
    needs: [changes, lint-and-test]
    if: |
      always() &&
      needs.changes.outputs.code == 'true' &&
      needs.lint-and-test.result == 'success' &&
      (github.event_name == 'push' || github.event.pull_request.draft == false)
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            platform: mac
          - os: ubuntu-24.04
            platform: linux
          - os: ubuntu-24.04-arm
            platform: linux
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
          run_install: false
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec electron-vite build
      - run: pnpm exec electron-builder --${{ matrix.platform }} --publish never

  ci-ok:
    # Use this as the single required status check in branch protection.
    # Always runs and reports success only if no upstream job failed.
    if: always()
    needs: [lint-and-test, build]
    runs-on: ubuntu-24.04
    steps:
      - run: |
          [[ "${{ needs.lint-and-test.result }}" =~ ^(success|skipped)$ ]] &&
          [[ "${{ needs.build.result }}" =~ ^(success|skipped)$ ]]
